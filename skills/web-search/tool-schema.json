{
  "name": "web_search",
  "description": "Searches the web for a given query and returns a ranked list of results with titles, URLs, and snippets. Use for current information beyond the model's training cutoff.",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query string."
      },
      "num_results": {
        "type": "integer",
        "description": "Number of results to return. Max 20. Defaults to 5.",
        "minimum": 1,
        "maximum": 20
      }
    },
    "required": ["query"]
  }
}
