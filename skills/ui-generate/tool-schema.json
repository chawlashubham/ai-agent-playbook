{
  "name": "ui_generate",
  "description": "Generates UI component code (HTML/CSS, Tailwind, JSX, or TSX) from a plain-language description. Returns source code and a usage example.",
  "input_schema": {
    "type": "object",
    "properties": {
      "description": {
        "type": "string",
        "description": "Plain-language description of the UI component to generate. Max 1000 characters."
      },
      "framework": {
        "type": "string",
        "enum": ["html", "tailwind", "jsx", "tsx"],
        "description": "Target output format. Defaults to 'html'."
      },
      "theme": {
        "type": "string",
        "enum": ["light", "dark", "system"],
        "description": "Color theme hint. Defaults to 'light'."
      },
      "responsive": {
        "type": "boolean",
        "description": "Whether to include responsive breakpoint styles. Defaults to true."
      }
    },
    "required": ["description"]
  }
}
