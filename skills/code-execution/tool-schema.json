{
  "name": "code_execution",
  "description": "Executes a Python or JavaScript code snippet in a sandboxed environment with no filesystem or network access. Returns stdout, stderr, and exit code.",
  "input_schema": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "description": "The code snippet to execute."
      },
      "language": {
        "type": "string",
        "enum": ["python", "javascript"],
        "description": "Programming language of the snippet."
      },
      "timeout_seconds": {
        "type": "integer",
        "description": "Maximum execution time in seconds. Defaults to 10. Hard cap is 30.",
        "minimum": 1,
        "maximum": 30
      }
    },
    "required": ["code", "language"]
  }
}
